{
  "name": "rw-multi-web-root",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "project"
  ],
  "scripts": {
    "build": "npm run build --workspace=project && node -e \"const fs = require('fs'); fs.cpSync('project/dist', 'dist', { recursive: true });\""
  },
  "dependencies": {
    "playwright": "^1.61.0"
  }
}
